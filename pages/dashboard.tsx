import React from 'react'
import Layout from '../components/Layout'

type Props = {}

export default function dashboard({}: Props) {
  return (
    <Layout>
      <div>dashboard</div>
    </Layout>
    
  )
}